Personal macOS configuration managed with chezmoi.

- **Local skills**: Keep project-specific agent skills in `.agents/` (not `dot_agents/`).
- **Secrets**: Extract credentials into template files (`.tmpl`) using environment variables.
